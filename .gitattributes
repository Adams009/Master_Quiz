# .gitattributes
# Set default behavior to automatically normalize line endings.
* text=auto

# Explicitly declare text files you want to always be normalized and converted to native line endings on checkout.
*.c text
*.h text

# Declare files that will have CRLF line endings on checkout.
*.sln text eol=crlf

# Declare files that will have LF line endings on checkout.
*.sh text eol=lf

# Auto detect text files and perform LF normalization
* text=auto

# Explicitly declare text files we want to always be normalized and converted to LF in repo
*.py text eol=lf
*.sh text eol=lf
*.md text eol=lf
*.ini text eol=lf


